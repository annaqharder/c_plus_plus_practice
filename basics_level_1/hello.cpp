#include <iostream>

int main()
{
    // 1) declaring a variable
    int a; 
    // 2) initializing the variable
    a = 2;

    // declare & initialize variable at the same time
    int b = a + 3; 

     std::cout << b << std::endl;

 
    return 0;
}